pangeo:
  jupyterhub:
    proxy:
      https:
        hosts:
          - staging.ocean.pangeo.io
        letsencrypt:
          contactEmail: raphael.dussin@gmail.com
      service:
        loadBalancerIP: 35.238.36.78
    hub:
      resources:
        requests:
          cpu: "0.1"
          memory: 0.25Gi
    scheduling:
      userScheduler:
        enabled: false
    singleuser:
      extraEnv:
        DASK_GATEWAY__ADDRESS: "https://staging.ocean.pangeo.io/services/dask-gateway/"
        DASK_GATEWAY__PROXY_ADDRESS: "tls://scheduler-public-ocean-staging-dask-gateway:8786"
    hub:
      services:
        dask-gateway:
          # This makes the gateway available at ${HUB_URL}/services/dask-gateway
          url: "http://web-public-ocean-staging-dask-gateway"
